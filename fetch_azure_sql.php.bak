<?php

$url = "https://example.com"; // URL to hit

// Initialize cURL
$curl = curl_init();

// Set cURL options
curl_setopt($curl, CURLOPT_URL, $url);
curl_setopt($curl, CURLOPT_RETURNTRANSFER, true);

// Execute cURL request and retrieve response
$response = curl_exec($curl);

?>
